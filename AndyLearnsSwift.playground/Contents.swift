import Cocoa

/// Ternary condition operator: a conditional that operates on 3 pieces of data. Let's us check a condition and return either one value or another value depending on the condition
let age = 25
let canVote = age >= 18 ? "You can vote!" : "You cannot vote!"
